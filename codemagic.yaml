workflows:
  android-debug:
    name: Android Debug

    environment:
      java: 17

    scripts:
      - chmod +x gradlew
      - ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/apk/debug/*.apk
